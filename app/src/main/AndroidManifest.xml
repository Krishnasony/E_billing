<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.krishanasony.e_billing">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Electricity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EbsSignin" />
        <activity android:name=".EbsSignup" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".Payment_HistoryActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".Bill_payActivity" />
        <activity android:name=".Customer_careActivity" />
        <activity android:name=".Pay" />
        <activity android:name=".Cong.Paymentdetails" />
        <activity android:name=".forgot_Pssd"></activity>
    </application>

</manifest>